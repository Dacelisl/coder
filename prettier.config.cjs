module.exports = {
  semi: true, // agrega punto y coma
  singleQuote: true, // usa comillas simples
  trailingComma: 'es5', // coma al final donde sea válido en ES5 (objetos, arrays, etc.)
  printWidth: 100, // ancho máximo de línea
  tabWidth: 2, // espacios por tabulación
  bracketSpacing: true, // { foo: bar } en lugar de {foo:bar}
  arrowParens: 'avoid', // (x) => x se convierte en x => x
};
